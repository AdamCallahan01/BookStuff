{# <script>
  const words = {{ words | dump | safe }};
  const randomWord = words[Math.floor(Math.random() * words.length)];

  document.addEventListener("DOMContentLoaded", () => {
    const el = document.getElementById("random-word");

    el.innerHTML = `
      <div class="word-card">
        <h2>${randomWord.word}</h2>
        <p>${randomWord.definition}</p>
      </div>
    `;
  });
</script>

<aside class="word-card">
  <h3>Random Word</h3>
  <p id="random-word">Loading...</p>
</aside> #}

<aside
  class="word-card"
  id="random-word-widget"
  data-words='{{ words | dump | escape }}'
>
  <h3>Random Word</h3>
  <p id="random-word">Loading...</p>
</aside>

<script src="/js/randomWord.js"></script>